var e=48e3;async function t(t){if(t.sampleRate===e)return n(t);let r=Math.ceil(t.duration*e),i=new OfflineAudioContext(t.numberOfChannels,r,e),a=i.createBufferSource();return a.buffer=t,a.connect(i.destination),a.start(),i.startRendering()}function n(e){let t=new AudioBuffer({numberOfChannels:e.numberOfChannels,length:e.length,sampleRate:e.sampleRate});for(let n=0;n<e.numberOfChannels;n++)t.copyToChannel(e.getChannelData(n).slice(),n);return t}export{t as normalizeTo48kHz};